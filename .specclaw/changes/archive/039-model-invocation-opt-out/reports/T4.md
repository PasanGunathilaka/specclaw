# T4 — L1: pass the collector path, not its stdout

All 15 spawn sites across the nine bf skills now `mkdir -p .specclaw/analysis/.collect`, redirect
their collector to a phase-named file, check the collector's exit status before spawning, and pass
the agent **the path**. Every key pointer the original bullet named is preserved verbatim
(`pending_questions.open`, `next_id`, `existing_questions`; `bank_path`, `new_sq_ids`, `adr_dir`,
`decisions_md`, `docs_present`; `module_map`/`architecture_md`).

All nine agent charters gained one clause in their existing **Collected facts (JSON)** input bullet:
the facts arrive as a file path read with `Read`, naming the exact `.collect/<phase>.json`.

**bf-clarify's file allocation is per collector invocation, not per spawn.** Mode A runs one
`specclaw-bf-clarify collect` whose output feeds three spawns (ingest, extract, bank) — they share
`clarify-extract.json`. Modes B and C run their own distinct collectors (`resolve-collect`,
`options-pack-collect`) and get their own files. Orchestrator re-checked this against the skill:
one collect command at Mode A, three pass-as-context bullets referencing it.

Commit `62cf3f0` — 18 files (9 skills + 9 charters), +71 / −58. `bin/`, the config template and
`references/model-invocation-map.md` untouched; `skills/bf-quality/SKILL.md` (T5's file) untouched.

**Orchestrator's independent verification** (the agent's own footer reported `Exit: 1` — a bare
`grep` with no matches legitimately exits 1, so the checks below are written to exit 0 on success
and were re-run by the orchestrator against the committed tree).

## Verification

Command: `n=$(grep -rl 'collected JSON\|stdout of Step' plugins/specclaw/skills/bf-*/SKILL.md | wc -l); test "$n" -eq 0 && echo "AC-1 PASS"; for f in analyze architecture baseline blueprint bootstrap clarify domain rebuild-plan ui; do grep -o '\.specclaw/analysis/\.collect/[a-zA-Z0-9-]*\.json' plugins/specclaw/skills/bf-$f/SKILL.md | sort | uniq -c; done`
Exit: 0

Output:

    files still matching: 0
    AC-1 PASS

    bf-analyze       2 .collect/analyze.json
    bf-architecture  2 .collect/architecture.json
    bf-baseline      2 .collect/baseline-design.json   2 .collect/baseline-harness.json
    bf-blueprint     2 .collect/blueprint.json
    bf-bootstrap     2 .collect/bootstrap.json
    bf-clarify       4 .collect/clarify-extract.json   2 .collect/clarify-options-pack.json   2 .collect/clarify-resolve.json
    bf-domain        2 .collect/domain.json
    bf-rebuild-plan  2 .collect/rebuild-plan.json
    bf-ui            2 .collect/ui-checklist.json      2 .collect/ui-extract.json

Every path appears twice — once in its collector redirect, once in its pass-as-context bullet —
except `clarify-extract.json` at 4 (one collect + three shared spawns), which is 15 pass sites in
total, matching FR-1 exactly.

Also run: `bash plugins/specclaw/tests/run-description-lint-tests.sh` → `description lint: no new
offences (39 baselined, 35 skills checked)`.
