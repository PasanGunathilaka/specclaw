# T7 — Register the suite and bump the version

Registered `run-narration-gate-tests.sh` in `.github/workflows/ci.yml`, alongside the other
per-push bash suites. Bumped `0.7.4 → 0.7.5` in both version files, kept in sync.

Checked registration **in both directions**, because the one-way check is what lets a suite go
unregistered: every suite named in a workflow exists on disk, and every `tests/run-*.sh` on disk is
named in a workflow. `run-trigger-tests.sh` is the only suite absent from `ci.yml` and it is not an
omission — it runs in `trigger-evals.yml`, the separate routing workflow the `ci.yml` comment
describes as deliberately not per-push.

## Verification

Command: `for f in $(grep -oE 'plugins/specclaw/tests/run-[a-z-]*\.sh' .github/workflows/ci.yml | sort -u); do test -f "$f" || echo MISSING; done; for f in plugins/specclaw/tests/run-*.sh; do grep -qrF "$f" .github/workflows/ || echo UNREGISTERED; done; python3 -c "import json; [json.load(open(f)) for f in (...)]"`
Exit: 0

Output:

    all registered suites exist
    all suites registered across .github/workflows/
      (run-trigger-tests.sh → trigger-evals.yml:76, by design)
    ci.yml steps: 32, narration step present: True
    plugin.json  "version": "0.7.5"
    marketplace  "version": "0.7.5"
    both JSON files parse
