# T1 — Audit and classify every bf spawn site

Created `plugins/specclaw/references/model-invocation-map.md`: 19 rows, one per `bf-*` Agent spawn
site, matching the expected per-skill distribution exactly. Each row cites an exact `SKILL.md:line`
plus a verbatim quote from that line or from the agent's charter in `plugins/specclaw/agents/`.

**Result: 1 `narration`, 18 `judgement`.** The single narration site is `bf-quality`, the pilot this
change implements. The Summary names `bf-blueprint`, `bf-clarify` bank mode and `bf-clarify` resolve
mode as the nearest analogs for any future extension, explicitly as a non-binding forward-looking
note rather than a reclassification — per the task's rule to classify `judgement` when unsure.
`bf-blueprint` is flagged borderline: its header verdict is bash-computed like bf-quality's, but the
bulk of its output still requires fresh synthesis from decision text.

Three citations were independently spot-checked against source by the orchestrator after the agent
reported (`bf-bootstrap:63`, `bf-quality:75`, `bf-replay:145`) and matched byte-for-byte.

Commit `bae464f` touches exactly one file.

## Verification

Command: `grep -cE '^\| [0-9]+ \|' plugins/specclaw/references/model-invocation-map.md && grep -oE '\*\*(narration|judgement)\*\*' plugins/specclaw/references/model-invocation-map.md | sort | uniq -c`
Exit: 0

Output:

    19
         18 **judgement**
          1 **narration**

Independent re-check (orchestrator):

    git show --stat bae464f  →  1 file changed, 115 insertions(+)
    sed -n '75p' skills/bf-quality/SKILL.md  →  quote matches the row-17 citation verbatim
