# T2 — Add the `bf:` config block

Inserted a top-level `bf:` block into `plugins/specclaw/templates/config.yaml`, immediately before
`# Verify settings`, carrying `narration: true` and the comment from design.md's Data Model section
verbatim — including the sentence that the switch saves tokens/money/wall-clock but **not**
main-session context, and the sentence that anything other than the literal `false` leaves
narration on.

Placed after `build:` and before `verify:` so the file still reads in lifecycle order.

Verified by extracting `yaml_val` from `bin/specclaw-build` and reading the new key plus four
neighbouring keys, to prove the new block does not shadow or disturb any existing section — the
exact failure that made `party` need its own reader.

Note for later tasks: the Bash tool's shell here is **zsh**, where `[[ =~ ]]` / `BASH_REMATCH` do
not behave as in bash. `yaml_val` must be exercised under `bash -c`.

## Verification

Command: `bash -c '. yv.sh; for k in bf.narration build.parallel_tasks party.enabled verify.e2e loop.enabled; do printf "%-22s = [%s]\n" "$k" "$(yaml_val plugins/specclaw/templates/config.yaml "$k")"; done'`
Exit: 0

Output:

    bf.narration           = [true]
    build.parallel_tasks   = [3]
    party.enabled          = [true]
    verify.e2e             = [last]
    loop.enabled           = [true]
