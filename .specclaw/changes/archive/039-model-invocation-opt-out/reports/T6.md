# T6 — Test suite for the narration gate

`plugins/specclaw/tests/run-narration-gate-tests.sh`, 411 lines, commit `fe5f890`, one file.
jq-free per the repo rule (JSON via `python3`; the anchored-region extraction is copied from the
collector's own `report_block_region` helper).

Six groups, 42 assertions at commit time (45 after T9 added Group G):

- **A** (19) — `yaml_val` section scoping with **decoy `narration:` keys in other blocks** and an
  inline trailing comment; the switch rule that only the literal `false` disables (`off`, `no`, `0`,
  empty, absent all stay ON); and the `--no-model` asymmetry, including that no token turns
  narration back on against `bf.narration: false`.
- **B** — end-to-end scratch-repo render, each anchored region byte-identical to its
  `report_blocks.<field>`, marker present, no `{{placeholder}}`, no instruction comment survives.
- **C** — `lint-report` exits 0 and prints a PASS verdict on the rendered report.
- **D** — the T8 regression, asserting **both** rc=0 and non-empty output, because the original
  bug's symptom was silence. The agent validated the assertion by patching a throwaway copy of the
  collector to remove the `|| true` and confirming it reproduces rc=1 with empty stdout and stderr.
- **E** — the repo-wide AC-1 grep.
- **F** — `specclaw-init` idempotency on the `.collect/` block.

No metric tools (`scc`/`lizard`/`jscpd`) are installed here, so a real `collect` run produces a
genuinely zero-entry QI registry — used as the real AC-11 fixture rather than a synthetic one. The
one-entry case is spliced in on top. Skip paths were verified to trigger loudly under a stripped
PATH rather than passing silently.

**Finding handed back, now fixed as T9:** `yaml_val` was *not* byte-identical between
`specclaw-build` and `specclaw-bf-quality-render`, despite the renderer's comment claiming it was.
`substitute()` was correctly identical.

## Verification

Command: `bash plugins/specclaw/tests/run-narration-gate-tests.sh && bash plugins/specclaw/tests/shellcheck-gate.sh`
Exit: 0

Output:

    PASS: 42   FAIL: 0   SKIP: 0
    shellcheck: no new findings (23 known, all in the baseline)

Re-run by the orchestrator after T9: `PASS: 45   FAIL: 0   SKIP: 0`.
