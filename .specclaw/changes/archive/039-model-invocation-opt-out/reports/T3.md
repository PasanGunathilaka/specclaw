# T3 — Gitignore the collector handoff directory

Added a second idempotent, grep-guarded `.gitignore` block to `plugins/specclaw/bin/specclaw-init`,
in the same shape as the existing `timeline.jsonl` block it now follows: fixed-string guard,
append once, `|| true` so a failed append never fails an init.

Ignores `.specclaw/analysis/.collect/`. The comment states why this is a handoff rather than a
record — regenerated every run, read once, 229 KB on this repo — which is what keeps it clear of
the repo's "never store a derived fact" constraint.

Verified against the **working-tree** script (`bash plugins/specclaw/bin/specclaw-init`), not the
`specclaw-init` on PATH, which resolves to the installed 0.7.3 plugin and would not have exercised
the edit. Also note `specclaw-init` takes the **project dir**, not the `.specclaw` dir.

AC-10: a second init against a repo that already carries the block adds nothing — still exactly one
entry. (A second init with `.specclaw/` still present refuses outright with `rc=1`, so the guard is
exercised by removing `.specclaw/` and keeping `.gitignore`.)

NFR-2: `tests/shellcheck-gate.sh` exits 0 with `shellcheck-baseline.txt` unmodified. The gate's
"these baseline entries no longer occur" note for `specclaw-loop SC2015` is pre-existing — it
prints identically on stashed `main`.

## Verification

Command: `bash plugins/specclaw/bin/specclaw-init "$S/t3" (twice, .specclaw removed between runs); grep -c '^\.specclaw/analysis/\.collect/$' .gitignore; bash plugins/specclaw/tests/shellcheck-gate.sh`
Exit: 0

Output:

    # specclaw: raw timing ledgers. timeline.md (the render) is committed.
    .specclaw/changes/*/timeline.jsonl
    .specclaw/changes/archive/*/timeline.jsonl

    # specclaw: bf-* collector handoff files. Regenerated every run.
    .specclaw/analysis/.collect/

    collect entries: 1
    gate rc=0   (23 known findings, all in the baseline; identical on main)
