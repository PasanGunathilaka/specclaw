# T2 — Add the Codex marketplace catalog and native SpecClaw manifest

Added the repository-native `chan4lk` Codex marketplace catalog and a native
manifest beside the canonical plugin. The manifest exposes the existing
`./skills/` tree and mirrors all shared metadata from the Claude manifest.

The focused contract now passes every package and skill-inventory assertion;
only its deliberately pending CI-registration assertions remain for T4.

## Verification

Command: `python3 -c "import json; json.load(open('.agents/plugins/marketplace.json')); json.load(open('plugins/specclaw/.codex-plugin/plugin.json'))"`
Exit: 0
