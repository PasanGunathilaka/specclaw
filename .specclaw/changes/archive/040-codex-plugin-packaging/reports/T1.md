# T1 — Add failing native Codex package-contract validation

Added the focused offline package-contract suite. It checks for the native
marketplace and plugin manifests, validates the marketplace source path and
manifest metadata parity once present, verifies the canonical skill inventory,
and pins CI registration and JSON parsing.

Before implementation, the suite correctly reports the four missing packaging
requirements: marketplace manifest, native manifest, test-job registration,
and JSON-job registration.

## Verification

Command: `if bash plugins/specclaw/tests/run-codex-plugin-tests.sh; then exit 1; fi`
Exit: 0
