# T5 — Validate package behavior and regression boundaries

The native package contract, existing checkout-local adapter contract, skill
description lint, JSON parsing, whitespace check, and shellcheck gate all pass.
The CLI smoke test was intentionally not run: its configured marketplace/cache
location is operator-global, while this change requires an isolated test home.
The static contract covers package discovery without mutating user state.

## Verification

Command: `bash plugins/specclaw/tests/run-codex-plugin-tests.sh && bash plugins/specclaw/tests/run-codex-skill-tests.sh && bash plugins/specclaw/tests/run-description-lint-tests.sh && bash plugins/specclaw/tests/shellcheck-gate.sh`
Exit: 0
